## 安装软件报错？

容器内硬链接不可用所致，添加 proot 参数解决：

```
--link2symlink
```

编辑镜像 → proot 参数 → 添加 → 保存，重进终端。

> 下载镜像导入已默认带此参数。

## 终端按键异常（方向键显示 ^[[A 等）？

当前 shell 是 sh，无行编辑能力。编辑容器，Shell 改为：

```
/bin/bash
```

bash 自带方向键、历史、补全。
